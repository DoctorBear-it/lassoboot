% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stability.R
\name{lb_correlated_pairs}
\alias{lb_correlated_pairs}
\title{Identify correlated predictor pairs with split selection probabilities}
\usage{
lb_correlated_pairs(boot, cor_threshold = 0.7, prob_threshold = 0.5)
}
\arguments{
\item{boot}{An \code{lb_boot} object.}

\item{cor_threshold}{Correlation threshold above which pairs are examined.
Default \code{0.7}.}

\item{prob_threshold}{Individual selection probability threshold below which
a term is considered "split". Default \code{0.5}.}
}
\value{
A tibble of flagged pairs with columns \code{term_1}, \code{term_2},
\code{correlation}, \code{prob_1}, \code{prob_2}, \code{prob_either}.
}
\description{
Detects the "lasso picked one of two correlated predictors at random"
pathology. Flags pairs where \code{cor > cor_threshold} in the original data and
the individual selection probabilities are split (neither clearly dominant)
while the joint either-one probability is high.
}
\examples{
set.seed(1)
n  <- 40
df <- data.frame(x1 = rnorm(n), x2 = rnorm(n), x3 = rnorm(n))
df$y <- 2 * df$x1 + rnorm(n)
spec <- suppressMessages(lb_spec(y ~ x1 + x2 + x3, data = df))
boot <- lb_bootstrap(spec, B = 5)
lb_correlated_pairs(boot)
}
