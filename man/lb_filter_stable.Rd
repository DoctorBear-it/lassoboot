% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stability.R
\name{lb_filter_stable}
\alias{lb_filter_stable}
\title{Filter tidy bootstrap output on stability criteria}
\usage{
lb_filter_stable(
  tidy_df,
  min_selection_prob = NULL,
  min_stability_score = NULL,
  quantiles_exclude_zero = FALSE
)
}
\arguments{
\item{tidy_df}{A tibble from \code{\link[=tidy.lb_boot]{tidy.lb_boot()}}.}

\item{min_selection_prob}{Minimum selection probability threshold. Rows with
\code{selection_prob < min_selection_prob} are excluded. Default \code{NULL} (no
filter on this criterion).}

\item{min_stability_score}{Minimum stability score threshold. Rows with
\code{stability_score < min_stability_score} are excluded. Default \code{NULL} (no
filter on this criterion).}

\item{quantiles_exclude_zero}{Logical. If \code{TRUE}, exclude rows where the
default 95 \% quantile interval (\code{q025} to \code{q975}) does not exclude zero
(i.e. rows where \code{q025 <= 0} AND \code{q975 >= 0}). Default \code{FALSE}.}
}
\value{
A logical vector the same length as \code{nrow(tidy_df)}.
}
\description{
A convenience predicate for use inside \code{\link[dplyr:filter]{dplyr::filter()}}. Applies one or
more stability criteria to a tibble from \code{\link[=tidy.lb_boot]{tidy.lb_boot()}}. Unlike the
deprecated \code{\link[=lb_is_significant]{lb_is_significant()}}, each criterion is an independent named
argument that can be combined in any combination.
}
\examples{
set.seed(1)
n  <- 40
df <- data.frame(x1 = rnorm(n), x2 = rnorm(n), x3 = rnorm(n))
df$y <- 2 * df$x1 + rnorm(n)
spec <- suppressMessages(lb_spec(y ~ x1 + x2 + x3, data = df))
boot <- lb_bootstrap(spec, B = 20)
td <- tidy(boot)
# Keep only terms selected in the majority of replicates
td[lb_filter_stable(td, min_selection_prob = 0.5), ]
# Combine criteria
td[lb_filter_stable(td, min_selection_prob = 0.5, min_stability_score = 0.4), ]
}
