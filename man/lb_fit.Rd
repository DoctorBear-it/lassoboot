% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit.R
\name{lb_fit}
\alias{lb_fit}
\title{Fit an initial lasso model and select lambda}
\usage{
lb_fit(spec)
}
\arguments{
\item{spec}{An \code{lb_spec} object from \code{\link[=lb_spec]{lb_spec()}}.}
}
\value{
An \code{lb_fit} object containing the parent \code{lb_spec}, engine fit
object, selected lambda scalar, lambda path vector, CV results, and
residual sigma estimate(s).
}
\description{
Builds the design matrix, runs (repeated) cross-validation to select lambda,
fits the final model via the spec's engine, and estimates the residual
standard error. The result is stored in an \code{lb_fit} object and passed to
\code{\link[=lb_bootstrap]{lb_bootstrap()}}.
}
