% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{autoplot.lb_boot}
\alias{autoplot.lb_boot}
\title{Plot bootstrap lasso output}
\usage{
\method{autoplot}{lb_boot}(
  object,
  type = c("coefficients", "selection", "stability", "interactions", "complexity",
    "prediction"),
  ...
)
}
\arguments{
\item{object}{An \code{lb_boot} object.}

\item{type}{Plot type: \code{"coefficients"} (default), \code{"selection"},
\code{"stability"}, \code{"interactions"}, \code{"complexity"}, or \code{"prediction"}.}

\item{...}{Passed to the underlying \verb{lb_plot_*()} function. For
\code{type = "prediction"}, \code{focal} (a column name string) must be supplied
here.}
}
\value{
A \code{ggplot} object.
}
\description{
Dispatcher for all \verb{lb_plot_*()} functions. Each type delegates to the
corresponding named function and returns a plain \code{ggplot} object (no
\code{print()}, no side effects). All inherit data through \code{\link[=tidy.lb_boot]{tidy.lb_boot()}} so
user-supplied filtering works identically.
}
\examples{
\donttest{
set.seed(1)
n  <- 40
df <- data.frame(x1 = rnorm(n), x2 = rnorm(n), x3 = rnorm(n))
df$y <- 2 * df$x1 + rnorm(n)
spec <- suppressMessages(lb_spec(y ~ x1 + x2 + x3, data = df))
boot <- lb_bootstrap(spec, B = 5)
autoplot(boot)
autoplot(boot, type = "selection")
}
}
