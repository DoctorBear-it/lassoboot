% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spec.R
\name{lb_spec}
\alias{lb_spec}
\title{Specify a lasso bootstrap model}
\usage{
lb_spec(
  formula,
  data,
  uncertainty = NULL,
  derive = NULL,
  constraints = NULL,
  folds = lb_folds_kfold(10),
  engine = lb_engine_glmnet(),
  control = lb_control()
)
}
\arguments{
\item{formula}{A model formula. The response must be numeric. Random-effect
terms (e.g. \code{(1 | group)}) are not supported by the glmnet engine and
raise an informative error.}

\item{data}{A data frame or tibble.}

\item{uncertainty}{A measurement uncertainty specification from
\code{\link[=lb_uncertainty]{lb_uncertainty()}}, or \code{NULL} for no predictor perturbation.
Default \code{NULL}.}

\item{derive}{A list of \code{\link[=lb_derive]{lb_derive()}} objects evaluated in declaration order
inside the bootstrap loop after uncertainty injection and constraint
clipping. Default \code{NULL}.}

\item{constraints}{A \code{\link[=lb_constraints]{lb_constraints()}} object specifying per-column
clipping bounds, or \code{NULL} to infer defaults (non-negative if all observed
values >= 0, unconstrained otherwise). Default \code{NULL}. Inferred defaults
are reported once at construction.}

\item{folds}{A fold-generator function from \verb{lb_folds_*()}. Default
\code{\link[=lb_folds_kfold]{lb_folds_kfold()}}.}

\item{engine}{An engine from \verb{lb_engine_*()}. Default \code{\link[=lb_engine_glmnet]{lb_engine_glmnet()}}.}

\item{control}{A \code{\link[=lb_control]{lb_control()}} object. Default \code{\link[=lb_control]{lb_control()}}.}
}
\value{
An \code{lb_spec} object.
}
\description{
The primary entry point for \code{lassoboot}. Declares the formula, data,
measurement uncertainty, derived quantities, per-column constraints,
cross-validation fold scheme, engine, and tuning control for the subsequent
bootstrap. Validates all inputs immediately and emits informative errors
that name domain quantities, not statistical operations.
}
\examples{
spec <- suppressMessages(lb_spec(strength_MPa ~ clayHOH + alumina, data = concrete))
print(spec)
}
