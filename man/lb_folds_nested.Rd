% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/folds.R
\name{lb_folds_nested}
\alias{lb_folds_nested}
\title{Nested fold generator with outer grouping and optional inner stratification}
\usage{
lb_folds_nested(outer, inner = NULL, k_outer = 5, k_inner = NULL)
}
\arguments{
\item{outer}{Column name (string) defining the outer grouping (no leakage).}

\item{inner}{Column name (string) for inner stratification within each fold,
or \code{NULL}. Default \code{NULL}.}

\item{k_outer}{Number of outer folds. Default \code{5}.}

\item{k_inner}{Reserved for future sub-folding; ignored in v0.1.}
}
\value{
A fold-generator closure \verb{function(data) -> integer vector}.
}
\description{
Places whole outer groups in the same fold (no leakage of group identity
across train/test) and stratifies within-fold composition by \code{inner} so
each fold sees a balanced distribution of the inner variable.
}
