% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bootstrap.R
\name{lb_bootstrap}
\alias{lb_bootstrap}
\title{Run the parametric bootstrap}
\usage{
lb_bootstrap(spec_or_fit, B = 1000, ...)
}
\arguments{
\item{spec_or_fit}{An \code{lb_spec} or \code{lb_fit} object. When an \code{lb_spec} is
supplied, \code{\link[=lb_fit]{lb_fit()}} is called first. Supplying an \code{lb_fit} skips that
step and is useful for inspecting the initial fit before committing to a
large bootstrap run.}

\item{B}{Number of bootstrap iterations. Default \code{1000}.}

\item{...}{Additional arguments passed to the engine.}
}
\value{
An \code{lb_boot} object containing the parent \code{lb_fit}, a tidy
coefficient tibble (B x p_selected rows), optional full-path coefficient
matrix, optional stored fit objects, the effective seed, and timings.
}
\description{
For each of \code{B} iterations: perturbs predictors using the declared
measurement uncertainty, applies constraints, re-evaluates derived
quantities, generates a parametric response from the original fit, refits
via the engine, and stores coefficients (and optionally the full path and
fit objects). Returns an \code{lb_boot} object.
}
