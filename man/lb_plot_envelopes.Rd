% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{lb_plot_envelopes}
\alias{lb_plot_envelopes}
\title{Flagship measurement-uncertainty-propagated prediction envelope plot}
\usage{
lb_plot_envelopes(
  boot,
  focal,
  group = NULL,
  facet = NULL,
  data = NULL,
  hlines = NULL,
  hline_types = NULL,
  xlim = NULL,
  ylim = NULL,
  n = 100L,
  alpha_ribbon = 0.25,
  label_x = NULL,
  label_y = NULL,
  interval = c("confidence", "prediction", "both"),
  ...
)
}
\arguments{
\item{boot}{An \code{lb_boot} object.}

\item{focal}{Name of the focal (x-axis) predictor (string). Passed to
\code{\link[=lb_grid]{lb_grid()}}.}

\item{group}{String column name to map to color, fill, and linetype
aesthetics, or \code{NULL}. Typically a grouping variable such as a replacement
level.}

\item{facet}{A \code{vars()} expression for faceting (e.g. \code{vars(Fineness, Alumina_Grade)}), or \code{NULL} for no faceting.}

\item{data}{A data frame to overlay as raw data points, or \code{NULL}. Must
contain the \code{focal} column and the response variable.}

\item{hlines}{Numeric vector of y-values for horizontal reference lines, or
\code{NULL}. The first value is drawn solid; subsequent values are dashed.
Override with \code{hline_types}.}

\item{hline_types}{Character vector of ggplot2 linetype values, one per
element of \code{hlines}. Default: first is \code{"solid"}, rest are \code{"dashed"}.}

\item{xlim}{Length-2 numeric or \code{NULL}. Passed to
\code{ggplot2::coord_cartesian()}.}

\item{ylim}{Length-2 numeric or \code{NULL}. Passed to
\code{ggplot2::coord_cartesian()}.}

\item{n}{Number of grid points along the focal axis. Passed to \code{\link[=lb_grid]{lb_grid()}}.
Default \code{100}.}

\item{alpha_ribbon}{Alpha for ribbon fill. Default \code{0.25}.}

\item{label_x}{X-axis label string, or \code{NULL} to use the focal column name.}

\item{label_y}{Y-axis label string, or \code{NULL} to use the response variable
name from the formula.}

\item{interval}{One of \code{"confidence"} (default), \code{"prediction"}, or \code{"both"}.
\code{"confidence"} shows where the model's mean function lives under
measurement-uncertainty perturbation. \code{"prediction"} shows where a single
new observation would land (confidence + residual scatter). \code{"both"} draws
an outer prediction ribbon (low alpha) and an inner confidence ribbon
(higher alpha) with the line on top.}

\item{...}{Additional arguments passed to \code{\link[=lb_grid]{lb_grid()}} (e.g.
\code{clip_to_observed}, \code{at}, \code{extrapolate}).}
}
\value{
A \code{ggplot} object. No side effects; add layers with \code{+}.
}
\description{
Produces per-combination prediction ribbons over a focal predictor, with
optional faceting, raw data overlay, and horizontal reference lines. This is
the recommended visualization for communicating bootstrap results to an
applied audience.
}
\details{
The user adds domain-specific binning columns to the prediction grid (returned
by \code{\link[=lb_grid]{lb_grid()}}) and to the raw data before calling this function; the function
does not guess what binning makes sense. Aesthetic overrides and additional
ggplot2 layers can be added to the returned object with \code{+}.
}
\examples{
\donttest{
set.seed(1)
n  <- 40
df <- data.frame(x1 = rnorm(n), x2 = rnorm(n), x3 = rnorm(n))
df$y <- 2 * df$x1 + rnorm(n)
spec <- suppressMessages(lb_spec(y ~ x1 + x2 + x3, data = df))
boot <- lb_bootstrap(spec, B = 5)
lb_plot_envelopes(boot, focal = "x1")
}
}
