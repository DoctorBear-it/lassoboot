% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predict.R
\name{predict.lb_boot}
\alias{predict.lb_boot}
\title{Predict from a bootstrap lasso fit}
\usage{
\method{predict}{lb_boot}(
  object,
  newdata = NULL,
  type = c("response", "coef"),
  interval = c("none", "confidence"),
  level = 0.95,
  B_sub = NULL,
  ...
)
}
\arguments{
\item{object}{An \code{lb_boot} object.}

\item{newdata}{A data frame for out-of-sample prediction, or \code{NULL} to
predict on training data. Default \code{NULL}.}

\item{type}{\code{"response"} (default) or \code{"coef"}.}

\item{interval}{\code{"none"} (default) or \code{"confidence"}.}

\item{level}{Confidence level. Default \code{0.95}.}

\item{B_sub}{Subsample this many bootstrap iterations for speed, or \code{NULL}
to use all. Default \code{NULL}.}

\item{...}{Unused; for S3 compatibility.}
}
\value{
A tibble with \code{.fitted} and, if \code{interval = "confidence"}, \code{.lower}
and \code{.upper}.
}
\description{
Reconstructs predictions from the stored coefficient matrix via direct
matrix multiplication (exact for Gaussian linear glmnet; the default fast
path). Does not require \code{keep_models = TRUE} for standard prediction.
The internal pipeline is matrix-based: no \code{nest()}/\code{unnest()} in the hot
path; summaries computed via \code{matrixStats::rowQuantiles}.
}
\examples{
set.seed(1)
n  <- 40
df <- data.frame(x1 = rnorm(n), x2 = rnorm(n), x3 = rnorm(n))
df$y <- 2 * df$x1 + rnorm(n)
spec <- suppressMessages(lb_spec(y ~ x1 + x2 + x3, data = df))
boot <- lb_bootstrap(spec, B = 5)
predict(boot, interval = "confidence")
}
